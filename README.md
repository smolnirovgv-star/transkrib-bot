# Transkrib Telegram Bot

## Запуск

1. Скопируй `.env.example` в `.env`
2. Заполни `TELEGRAM_BOT_TOKEN`
3. `pip install -r requirements.txt`
4. `python bot.py`

## Деплой на Render

Используй `render.yaml` — тип `worker`.
Добавь `TELEGRAM_BOT_TOKEN` в env vars на Render.
